#include "waves.h"

// Test for draw_waves
int main(int argc, char *argv[]) {
    draw_waves(3, 0, 0);
    draw_waves(3, 2, 5);
    draw_waves(5, 0, 0);
    draw_waves(3, 1, 1);
    draw_waves(10, 0, 0);
    return 0;
}
